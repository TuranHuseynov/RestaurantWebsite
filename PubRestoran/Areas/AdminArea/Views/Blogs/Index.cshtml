@model IEnumerable<PubRestoran.Models.Blog>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.blog_text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.blog_context)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.blog_text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.blog_context)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.blog_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.blog_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.blog_id })
        </td>
    </tr>
}

</table>
